import React from 'react';
import Header from '../header'
import Footer from '../footer'

const Login = (props) => {
    return(
        <div>
            <Header />
            <p>로그인페이지</p>
            <Footer />
        </div>
    )
}

export default Login;